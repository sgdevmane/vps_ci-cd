global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: 'vps-ci-cd-staging'
    metrics_path: '/api/metrics'
    static_configs:
      - targets: ['backend-staging:3000']
        labels:
          app: 'vps-ci-cd'
          environment: 'staging'