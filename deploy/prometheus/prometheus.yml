global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: 'vps-ci-cd-backends'
    metrics_path: '/api/metrics'
    static_configs:
      - targets: ['backend-1:3000', 'backend-2:3000']
        labels:
          app: 'vps-ci-cd'
          environment: 'production'
